{
  "//": "TEMPLATE for E:\\inetpub\\DataVerification\\api\\appsettings.Production.json",
  "//1": "An alternative to web.config environment variables, and the more robust one: this file is",
  "//2": "not in the repository, is never generated by dotnet publish, and is excluded by the",
  "//3": "robocopy line in the README, so it survives every update. Loaded automatically because",
  "//4": "ASPNETCORE_ENVIRONMENT is Production (that is also the default when the variable is unset).",
  "//5": "Values here override appsettings.json; a web.config environment variable overrides both.",
  "//6": "Delete the keys you do not need, and delete every // line. JSON has no comments - these are",
  "//7": "just unused keys, harmless but noisy. Save as UTF-8 without BOM.",

  "ConnectionStrings": {
    "//": "REQUIRED. appsettings.json is committed pointing at 172.201.83.229 / VData_Prod.",
    "DefaultConnection": "Server=SQLHOST;Database=VData_Prod;User Id=dv_app;Password=REPLACE_ME;TrustServerCertificate=True;MultipleActiveResultSets=true"
  },

  "Jwt": {
    "//": "REQUIRED, 32+ characters. The committed placeholder is public - replace it.",
    "SigningKey": "REPLACE_WITH_A_32_PLUS_CHARACTER_SECRET"
  },

  "Security": {
    "//": "REQUIRED for the Settings page and for revealing order passwords. 32 bytes base64.",
    "//generate": "$b = New-Object byte[] 32; [System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($b); [Convert]::ToBase64String($b)",
    "//warning": "BACK THIS UP. Change it and everything encrypted under it becomes unreadable.",
    "EncryptionKey": "REPLACE_WITH_32_BYTE_BASE64"
  },

  "Storage": {
    "//": "Absolute path, so uploads survive a redeploy.",
    "RootPath": "E:\\DataVerification\\storage"
  },

  "Cors": {
    "AllowedOrigins": [
      "https://vdata.nen-global.org",
      "https://vdataadmin.nen-global.org"
    ]
  },

  "App": {
    "WebUrl": "https://vdata.nen-global.org",
    "AdminUrl": "https://vdataadmin.nen-global.org"
  },

  "SendGrid": {
    "//": "Fallback only. The live key is set on the admin Settings page and stored encrypted in",
    "//2": "the database, which takes precedence over this value.",
    "Enabled": true,
    "ApiKey": ""
  }
}
